name: Restrict student commits

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      from_sha:
        description: 'Optional: Basis-Commit für den Vergleich (Standard: HEAD^)'
        required: false
      to_sha:
        description: 'Optional: Ziel-Commit für den Vergleich (Standard: HEAD)'
        required: false
      actor:
        description: 'Optional: GitHub-Login, der geprüft werden soll (überschreibt github.actor)'
        required: false

jobs:
  check-paths:
    name: 🔒 Restrict student commits
    runs-on: ubuntu-latest

    steps:
      # 🧩 Repository auschecken (vollständig)
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 🩺 Diagnose – zeigt Locale und Git-Version
      - name: Check runner environment
        run: |
          echo "🌍 Aktuelle Locale:"
          locale
          echo
          echo "💻 Git-Version:"
          git --version
          echo
          echo "🔤 Encoding-Test:"
          echo "Übung_Ärger_Ökonom.py"
          echo

      # ✅ Hauptprüfung
      - name: Verify changed file paths
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          EVENT_BEFORE: ${{ github.event.before }}
          EVENT_SHA: ${{ github.sha }}
          INPUT_FROM_SHA: ${{ inputs.from_sha }}
          INPUT_TO_SHA: ${{ inputs.to_sha }}
          INPUT_ACTOR: ${{ inputs.actor }}
        run: |
          # --- UTF-8-Locale aktivieren ---
          export LANG=C.UTF-8
          export LC_ALL=C.UTF-8

          echo "👤 Benutzer (github.actor): $GITHUB_ACTOR"
          echo "🔍 Branch:  $GITHUB_REF_NAME"
          echo "📦 Event:   $GITHUB_EVENT_NAME"
          echo

          # --- Commit-Vergleich bestimmen ---
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ]; then
            FROM="$INPUT_FROM_SHA"
            TO="$INPUT_TO_SHA"

            # Standard: Vergleich HEAD^ -> HEAD
            if [ -z "$TO" ]; then
              TO="HEAD"
            fi
            if [ -z "$FROM" ]; then
              FROM="${TO}^"
            fi

            echo "ℹ️ workflow_dispatch: prüfe Diff von $FROM bis $TO"
            CHANGED=$(git -c core.quotepath=off diff --name-only "$FROM" "$TO")

          else
            # Push / Pull Request
            if [ "$EVENT_BEFORE" = "0000000000000000000000000000000000000000" ]; then
              echo "ℹ️ Neuer Branch oder erster Commit – prüfe nur neue Dateien (HEAD)."
              CHANGED=$(git -c core.quotepath=off diff-tree --no-commit-id --name-only -r "$EVENT_SHA")
            else
              echo "ℹ️ Versuche, EVENT_BEFORE-Commit nachzuladen (wichtig bei Rebase/Force-Push)."
              git fetch --no-tags origin "$EVENT_BEFORE" || true

              if git cat-file -e "$EVENT_BEFORE^{commit}" 2>/dev/null; then
                echo "ℹ️ Prüfe Diff von $EVENT_BEFORE bis $EVENT_SHA"
                CHANGED=$(git -c core.quotepath=off diff --name-only "$EVENT_BEFORE" "$EVENT_SHA")
              else
                echo "⚠️ EVENT_BEFORE-Commit $EVENT_BEFORE ist lokal nicht verfügbar (z.B. nach Rebase/Force-Push)."
                echo "ℹ️ Fallback 1: geänderte Dateien aus Push-Event lesen (falls verfügbar)."

                if command -v jq >/dev/null 2>&1; then
                  CHANGED=$(jq -r '.commits[]? | .added[]?, .modified[]?, .removed[]?' \
                    "$GITHUB_EVENT_PATH" 2>/dev/null | sort -u)
                fi

                if [ -z "$CHANGED" ]; then
                  echo "ℹ️ Fallback 2: prüfe nur Dateien des neuen Commits $EVENT_SHA."
                  CHANGED=$(git -c core.quotepath=off diff-tree --no-commit-id --name-only -r "$EVENT_SHA")
                fi
              fi
            fi
          fi

          echo "📄 Geänderte / hinzugefügte Dateien:"
          echo "$CHANGED"
          echo

          # Wenn nichts geändert wurde, ist alles gut
          if [ -z "$CHANGED" ]; then
            echo "✅ Keine geänderten Dateien gefunden."
            exit 0
          fi

          # --- Actor bestimmen (ggf. Override bei workflow_dispatch) ---
          actor="$GITHUB_ACTOR"
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ] && [ -n "$INPUT_ACTOR" ]; then
            echo "ℹ️ Actor-Override aktiv: $INPUT_ACTOR"
            actor="$INPUT_ACTOR"
          fi

          # --- Lehrer-Ausnahme ---
          if [ "$actor" = "BerndDonner" ]; then
            echo "🧑‍🏫 Lehrer erkannt – keine Pfadprüfung erforderlich."
            exit 0
          fi

          # --- Actor-Ordner / erlaubte Pfade ---
          echo "👤 Erwarteter Benutzer-Ordner: ${actor}/"
          echo

          # ^...   = nur am Anfang der Zeile
          # actor/ = persönlicher Ordner
          # common/, shared/ = gemeinsame Ordner
          # README.md = global erlaubt
          # $        = Leerzeilen (zur Sicherheit)
          allowed_prefixes="^(${actor}/|common/|shared/|README\.md|$)"

          echo "🧩 Erlaubte Pfad-Präfixe (Regex): $allowed_prefixes"
          echo

          # --- Prüfen auf Verstöße ---
          violations=$(echo "$CHANGED" | grep -Ev "$allowed_prefixes" || true)

          if [ -n "$violations" ]; then
            echo "❌ Commit enthält Dateien außerhalb deines Verzeichnisses!"
            echo "👤 Erlaubt ist nur: ${actor}/ (plus ggf. common/, shared/, README.md)"
            echo "🚫 Nicht erlaubte Dateien:"
            echo "$violations"
            echo
            echo "💡 Hinweis: Benenne dein persönliches Verzeichnis exakt wie dein GitHub-Login (${actor})"
            exit 1
          fi

          echo "✅ Alle Änderungen befinden sich im erlaubten Bereich (${actor}/ oder gemeinsame Bereiche)."
          
      # 🧾 Debug: Event-Payload immer als Artifact sichern (für seltene, schwer reproduzierbare Fälle)
      - name: Save GitHub event payload (event.json)
        if: always()
        run: |
          mkdir -p artifacts
          cp "$GITHUB_EVENT_PATH" "artifacts/event_${GITHUB_EVENT_NAME}_${GITHUB_REF_NAME}_${GITHUB_SHA}.json"
          echo "Saved: artifacts/event_${GITHUB_EVENT_NAME}_${GITHUB_REF_NAME}_${GITHUB_SHA}.json"
          ls -lah artifacts

      - name: Upload event payload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: event-json-${{ github.run_number }}
          path: artifacts/
          retention-days: 30
